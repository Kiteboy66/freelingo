# Frontend service. Use the backend service's Railway private domain.
BACKEND_URL=http://backend.railway.internal:8000
# Public backend domain, baked into the browser bundle for WebSocket voice sessions.
# Set this after Railway generates the backend domain, then redeploy the frontend.
NEXT_PUBLIC_API_URL=https://YOUR-BACKEND-DOMAIN
PORT=3000
